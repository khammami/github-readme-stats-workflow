# Hello Stats Card

This repository is used to test the GitHub Action for generating stats cards for GitHub profiles. The workflow is defined in the `.github/workflows/grs.yml` file, which triggers on pushes to the main branch. The action generates a stats card using the `readme-tools/github-readme-stats-action` and updates the README.md file with the generated card.

![Stats Card](profile/stats.svg)
